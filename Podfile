platform :ios, '11.0'

target 'MindValley' do
  use_frameworks!
  
  # ignore all warnings from all pods
  inhibit_all_warnings!
  
  # Pods for MindValley
  pod "SkeletonView"
  pod 'HanekeSwift', :git => 'https://github.com/Haneke/HanekeSwift.git', :branch => 'master'
  
  target 'MindValleyTests' do
    inherit! :search_paths
  end
  
end
